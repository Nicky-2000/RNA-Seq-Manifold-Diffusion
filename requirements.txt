torch>=2.2
numpy>=1.24
scikit-learn>=1.3
anndata>=0.10     # optional for future RNA .h5ad loading
scipy>=1.10       # needed by anndata for sparse matrices

# Logging / config helpers (you may already have these)
pyyaml>=6.0

# Optional: progress bars
tqdm>=4.66
